# Copy to .env and fill appropriate values before running `docker compose build`/`docker compose push`

# MySQL credentials
MYSQL_ROOT_PASSWORD=change_me
MYSQL_DATABASE=jobportaldb
MYSQL_USER=jobportal
MYSQL_PASSWORD=change_me

# Optional: set these to your registry image names to push images
# e.g. BACKEND_IMAGE=yourdockerid/jobportal-backend:latest
#      FRONTEND_IMAGE=yourdockerid/jobportal-frontend:latest
BACKEND_IMAGE=job-portal-backend:latest
FRONTEND_IMAGE=job-portal-frontend:latest

# Other optional values used by backend/.env if you copy them here
# JWT_SECRET=...
# JWT_ISSUER=...
# REDIS_PORT=6379
# MAIL_HOST=...
# MAIL_PORT=587
# MAIL_USERNAME=...
# MAIL_PASSWORD=...
